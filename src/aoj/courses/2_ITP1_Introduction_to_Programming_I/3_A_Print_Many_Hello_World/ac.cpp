#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for(auto i=(decltype(n){0}); i<n; ++i)

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    rep(i, 1000) cout << "Hello World\n";

    return 0;
}