#include <bits/stdc++.h>
using namespace std;

typedef unsigned short int usi;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    usi x, i=0;
    while((cin >> x) && x) cout << "Case " << ++i << ": " << x << '\n';

    return 0;
}